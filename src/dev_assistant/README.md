# Dev Assistant - AI Ассистент для Flutter разработчиков

Умный помощник для работы с Flutter проектами, использующий RAG, MCP и Claude AI.

## Возможности

- 🔍 **RAG (Retrieval-Augmented Generation)** - индексирует документацию проекта
- 🌿 **MCP интеграция** - работает с Git репозиторием
- 💬 **Команда /help** - отвечает на вопросы о проекте
- 📚 **Умный поиск** - находит нужные фрагменты кода и документации

## Архитектура

```
dev_assistant/
├── backend/              # FastAPI сервер с RAG
│   ├── main.py          # Основной API
│   └── requirements.txt # Python зависимости
├── mcp_server/          # MCP сервер для Git
│   └── git_mcp.py       # Git операции
└── docs/                # Документация
    ├── flutter_structure.md
    └── code_style.md
```

## Установка

### 1. Backend (Python)

```bash
cd backend
pip install -r requirements.txt
```

### 2. Установите переменные окружения

```bash
export ANTHROPIC_API_KEY="your-api-key"
```

## Запуск

### Backend сервер (порт 8000)
```bash
cd backend
python main.py
```

### MCP Git сервер (порт 8001)
```bash
cd mcp_server
python git_mcp.py
```

## Использование

### 1. Индексация проекта

```bash
curl -X POST http://localhost:8000/index \
  -H "Content-Type: application/json" \
  -d '{"project_path": "/path/to/your/flutter/project"}'
```

### 2. Команда /help

```bash
curl -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -d '{"content": "/help структура проекта"}'
```

### 3. Получение текущей ветки Git

```bash
curl -X POST http://localhost:8001/git/branch \
  -H "Content-Type: application/json" \
  -d '{"repo_path": "/path/to/your/flutter/project"}'
```

## Примеры команд /help

- `/help` - показать список доступных команд
- `/help структура проекта` - объяснить структуру Flutter проекта
- `/help как добавить зависимость` - инструкция по добавлению зависимостей
- `/help правила стиля кода` - рекомендации по стилю
- `/help где находится main.dart` - поиск файлов в проекте
- `/help какие есть виджеты` - информация о виджетах проекта

## API Endpoints

### Backend (8000)

- `POST /index` - индексирует проект для RAG
- `POST /chat` - обрабатывает сообщения и команды
- `GET /health` - проверка статуса

### MCP Server (8001)

- `POST /git/branch` - текущая ветка и список всех веток
- `POST /git/status` - полный статус репозитория
- `POST /git/commits` - последние коммиты
- `POST /git/info` - вся информация о репозитории
- `GET /health` - проверка статуса

## Как это работает

### RAG Pipeline

1. **Индексация**: Загружаются документы (README, docs/*.md, pubspec.yaml, lib/*.dart)
2. **Chunking**: Документы разбиваются на чанки по ~500 слов
3. **Embeddings**: Создаются векторные представления через sentence-transformers
4. **FAISS Index**: Векторы индексируются для быстрого поиска
5. **Retrieval**: При запросе находятся топ-3 релевантных чанка
6. **Generation**: Claude генерирует ответ на основе найденного контекста

### MCP Integration

MCP сервер предоставляет информацию о Git репозитории:
- Текущая ветка
- Статус изменений
- История коммитов
- Информация о remote

## Технологии

- **FastAPI** - веб-фреймворк
- **LangChain** - RAG pipeline
- **Sentence Transformers** - векторные эмбеддинги
- **FAISS** - векторный поиск
- **Anthropic Claude** - генерация ответов
- **GitPython** - работа с Git

## Требования

- Python 3.9+
- Git репозиторий
- Anthropic API key

## Лицензия

MIT

## Автор

Создано для AI Challenge - День 20: Ассистент разработчика
