# 🚀 Инструкция по запуску Dev Assistant

## Быстрый старт

### 1. Установка зависимостей

```bash
cd dev_assistant/backend
pip install -r requirements.txt --break-system-packages
```

### 2. Установка переменной окружения

```bash
export ANTHROPIC_API_KEY="your-api-key-here"
```

### 3. Запуск сервисов

Откройте **два терминала**:

#### Терминал 1 - Backend (RAG + /help)
```bash
cd dev_assistant/backend
python main.py
```
Сервер запустится на `http://localhost:8000`

#### Терминал 2 - MCP Server (Git)
```bash
cd dev_assistant/mcp_server
python git_mcp.py
```
Сервер запустится на `http://localhost:8001`

---

## 📋 Демонстрация для зачета

### Минимальные требования:
- ✅ README + 1 папка project/docs в RAG
- ✅ MCP только для получения текущей ветки (git branch)
- ✅ Команда /help отвечает на вопросы о структуре проекта

### Вариант 1: Автоматическая демонстрация

```bash
cd dev_assistant
python test_assistant.py
```

Этот скрипт:
1. Проверит работу обоих сервисов
2. Проиндексирует тестовый проект
3. Протестирует команды /help
4. Покажет работу Git MCP

### Вариант 2: Ручная демонстрация через CLI

#### Шаг 1: Индексация проекта
```bash
cd dev_assistant
python cli.py index /Users/fehty/StudioProjects/rag_check
```

**Результат:** Система проиндексирует README.md, pubspec.yaml и файлы из lib/

#### Шаг 2: Команда /help - вопросы о проекте
```bash
# Общая структура
python cli.py help "структура проекта"

# Как добавить зависимость
python cli.py help "как добавить зависимость"

# Правила стиля
python cli.py help "правила стиля кода"

# Что такое StatefulWidget
python cli.py help "что такое StatefulWidget"
```

**Результат:** AI ответит на вопросы, используя проиндексированную документацию

#### Шаг 3: Git MCP - текущая ветка
```bash
python cli.py git-branch /Users/fehty/StudioProjects/rag_check
```

**Результат:** Покажет текущую ветку и список всех веток

#### Шаг 4: Git MCP - полный статус
```bash
python cli.py git-status /Users/fehty/StudioProjects/rag_check
```

**Результат:** Покажет статус репозитория (измененные файлы, staging и т.д.)

---

## 🧪 Тестирование через curl

### Индексация
```bash
curl -X POST http://localhost:8000/index \
  -H "Content-Type: application/json" \
  -d '{"project_path": "/Users/fehty/StudioProjects/rag_check"}'
```

### Команда /help
```bash
curl -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -d '{"content": "/help структура проекта"}'
```

### Git branch через MCP
```bash
curl -X POST http://localhost:8001/git/branch \
  -H "Content-Type: application/json" \
  -d '{"repo_path": "/Users/fehty/StudioProjects/rag_check"}'
```

### Git status через MCP
```bash
curl -X POST http://localhost:8001/git/status \
  -H "Content-Type: application/json" \
  -d '{"repo_path": "/Users/fehty/StudioProjects/rag_check"}'
```

---

## 📹 Что показать на видео

### 1. Запуск сервисов (10 сек)
- Показать запуск backend и mcp_server в двух терминалах

### 2. Индексация проекта (15 сек)
```bash
python cli.py index /Users/fehty/StudioProjects/rag_check
```
Показать, что индексируются:
- README.md
- pubspec.yaml
- lib/main.dart

### 3. Команды /help (30 сек)
```bash
python cli.py help "структура проекта"
python cli.py help "как добавить зависимость"
python cli.py help "правила стиля кода"
```
Показать, что AI отвечает на основе документации

### 4. Git MCP - текущая ветка (10 сек)
```bash
python cli.py git-branch /Users/fehty/StudioProjects/rag_check
```
Показать, что выводится текущая ветка

### 5. Итоговый экран (5 сек)
Показать структуру проекта:
```
dev_assistant/
├── backend/          # RAG система
├── mcp_server/       # Git интеграция
└── docs/            # Документация
```

**Общее время видео:** ~70 секунд

---

## ✅ Чек-лист для зачета

- [ ] README проиндексирован в RAG
- [ ] Папка docs/ проиндексирована в RAG
- [ ] MCP получает текущую ветку (git branch)
- [ ] Команда /help отвечает на вопросы о структуре проекта
- [ ] Видео демонстрации записано
- [ ] Код выложен (опционально)

---

## 🔧 Возможные проблемы

### Ошибка: "connection refused"
**Решение:** Убедитесь, что оба сервера запущены

### Ошибка: "ANTHROPIC_API_KEY not found"
**Решение:** Установите переменную окружения:
```bash
export ANTHROPIC_API_KEY="your-key"
```

### Ошибка: "Git repository not found"
**Решение:** Убедитесь, что путь указывает на Git репозиторий:
```bash
cd /Users/fehty/StudioProjects/rag_check
git init  # Если нужно
```

### Индекс пустой при /help
**Решение:** Сначала проиндексируйте проект:
```bash
python cli.py index /Users/fehty/StudioProjects/rag_check
```

---

## 📚 Дополнительные возможности

Система поддерживает больше, чем минимум для зачета:

### Полная информация о Git
```bash
curl -X POST http://localhost:8001/git/info \
  -H "Content-Type: application/json" \
  -d '{"repo_path": "/Users/fehty/StudioProjects/rag_check"}'
```

### История коммитов
```bash
curl -X POST http://localhost:8001/git/commits \
  -H "Content-Type: application/json" \
  -d '{"repo_path": "/Users/fehty/StudioProjects/rag_check"}'
```

### Проверка здоровья сервисов
```bash
curl http://localhost:8000/health
curl http://localhost:8001/health
```

---

## 🎯 Советы для лучшей демонстрации

1. **Добавьте в docs/ свои файлы** - чем больше документации, тем умнее ассистент
2. **Создайте несколько веток** в Git - чтобы показать работу MCP
3. **Задавайте конкретные вопросы** - например "где находится файл main.dart"
4. **Покажите источники** - система указывает, из каких файлов взята информация

---

## 📊 Архитектура решения

```
┌─────────────┐      ┌──────────────┐      ┌─────────────┐
│   Flutter   │─────▶│   Backend    │─────▶│    Claude   │
│   Project   │      │  (FastAPI)   │      │     API     │
└─────────────┘      └──────────────┘      └─────────────┘
                            │
                            ▼
                     ┌──────────────┐
                     │  RAG System  │
                     │  (FAISS +    │
                     │  Embeddings) │
                     └──────────────┘

┌─────────────┐      ┌──────────────┐
│     Git     │─────▶│ MCP Server   │
│   Repo      │      │  (FastAPI)   │
└─────────────┘      └──────────────┘
```

---

**Готово! Система полностью соответствует требованиям задания.**
