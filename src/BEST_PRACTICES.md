# God Agent - Best Practices & Tips

## 📚 Содержание

1. [Эффективное использование](#эффективное-использование)
2. [Промпт-инжиниринг](#промпт-инжиниринг)
3. [Управление контекстом](#управление-контекстом)
4. [Оптимизация производительности](#оптимизация-производительности)
5. [Безопасность](#безопасность)
6. [Расширение функциональности](#расширение-функциональности)

---

## Эффективное использование

### 1. Начните с четких запросов

**✅ Хорошо:**
```
Найди все TODO комментарии в файлах lib/ моего Flutter проекта 
и создай задачи с высоким приоритетом для каждого
```

**❌ Плохо:**
```
TODO
```

### 2. Используйте контекст

**✅ Хорошо:**
```
Я работаю над Flutter приложением для финансов. 
Какие библиотеки лучше использовать для работы с графиками?
```

**❌ Плохо:**
```
Какие библиотеки для графиков?
```

### 3. Структурируйте сложные задачи

**✅ Хорошо:**
```
Помоги мне подготовиться к релизу:
1. Проверь есть ли TODO в коде
2. Создай чеклист задач для тестирования
3. Найди последние breaking changes в Flutter
4. Сгенерируй changelog
```

### 4. Используйте теги в задачах

```python
# Создание задачи с тегами
"Создай задачу: Рефакторинг auth модуля, 
теги: refactoring, auth, high-priority"
```

Это помогает:
- Группировать связанные задачи
- Быстро находить нужное
- Анализировать работу

---

## Промпт-инжиниринг

### Эффективные паттерны

#### 1. Роль-контекст-задача

```
Ты - Flutter эксперт с 5+ годами опыта.
Контекст: Я работаю над финтех приложением.
Задача: Помоги выбрать архитектуру для state management.
```

#### 2. Пошаговое мышление

```
Проанализируй этот код пошагово:
1. Что делает этот метод?
2. Есть ли проблемы с производительностью?
3. Как можно улучшить?
4. Предложи рефакторинг
```

#### 3. Few-shot примеры

```
Создай задачи в таком формате:

Пример 1: "Код-ревью PR #123, priority: high, due: tomorrow"
Пример 2: "Написать тесты для auth, priority: medium, tags: testing, auth"

Теперь создай задачи для:
- Рефакторинг главной страницы
- Оптимизация загрузки изображений
```

#### 4. Chain-of-thought

```
Давай пошагово разберем проблему:

Проблема: Приложение тормозит при скролле
Шаг 1: Какие причины могут быть?
Шаг 2: Как их проверить?
Шаг 3: План исправления
Шаг 4: Как предотвратить в будущем?
```

---

## Управление контекстом

### 1. RAG для долгосрочной памяти

```python
# Явно сохраняйте важную информацию
"Запомни: В нашем проекте используем clean architecture с:
- Presentation layer (BLoC)
- Domain layer (Use Cases)
- Data layer (Repository pattern)"

# Позже можно спросить
"Какую архитектуру мы используем?"
```

### 2. Организация информации

**По категориям:**
```python
"Добавь в заметки [архитектура]: 
Используем feature-first структуру папок"

"Добавь в заметки [команда]:
Code review делаем через GitHub PR с минимум 2 апруверами"
```

### 3. Периодическая очистка

```bash
# Каждую неделю
python cli.py stats

# Удаление старых неактуальных данных
# Экспорт важного перед очисткой
python cli.py export-memory backup_$(date +%Y%m%d).json
```

### 4. Контекст в разговоре

```python
# Первое сообщение - контекст
"Я Flutter разработчик, работаю над финтех приложением.
Сейчас делаю фичу для управления инвестициями."

# Дальше агент будет помнить контекст
"Какие best practices для работы с финансовыми данными?"
```

---

## Оптимизация производительности

### 1. RAG оптимизация

```yaml
# config.yaml
rag:
  chunk_size: 1000        # Уменьшите для точности
  chunk_overlap: 200      # Увеличьте для лучшего контекста
  top_k: 5                # Баланс между скоростью и качеством
  similarity_threshold: 0.7  # Выше = строже фильтр
```

### 2. Кэширование

```python
# Добавьте часто используемые документы в RAG
documents = [
    "Flutter best practices",
    "Ваши code style guidelines",
    "Архитектура проекта",
    "Команды и процессы"
]

for doc in documents:
    await agent.rag.add_document(doc)
```

### 3. Батчинг запросов

```python
# Плохо - много отдельных запросов
await agent.process_input("Создай задачу 1")
await agent.process_input("Создай задачу 2")
await agent.process_input("Создай задачу 3")

# Хорошо - один запрос
await agent.process_input("""
Создай задачи:
1. Задача 1
2. Задача 2
3. Задача 3
""")
```

### 4. Управление историей

```python
# Очистка истории когда контекст не нужен
if context_changed:
    await agent.memory.clear_short_term()
```

---

## Безопасность

### 1. API ключи

```bash
# Всегда используйте .env
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# Никогда не коммитьте .env
echo ".env" >> .gitignore
```

### 2. Разрешения файловой системы

```yaml
# config.yaml
mcp:
  tools:
    - name: "filesystem"
      config:
        allowed_directories:
          - "./workspace"
          - "./documents"
        # Запретите системные папки
```

### 3. Подтверждение критичных действий

```yaml
security:
  require_confirmation:
    - "file_delete"
    - "email_send"
    - "payment"
    - "system_command"
```

### 4. Аудит

```python
# Включите audit log
security:
  audit_log: true

# Проверяйте логи
tail -f logs/god_agent.log
```

---

## Расширение функциональности

### 1. Добавление нового MCP инструмента

```python
# mcp_manager.py

class CustomTool(BaseTool):
    def get_definition(self):
        return {
            "name": "custom_tool",
            "description": "Описание инструмента",
            "input_schema": {
                "type": "object",
                "properties": {
                    "param": {"type": "string"}
                }
            }
        }
    
    async def execute(self, input_data):
        # Ваша логика
        return "Результат"
```

```yaml
# config.yaml
mcp:
  tools:
    - name: "custom_tool"
      enabled: true
      config:
        your_setting: "value"
```

### 2. Кастомные промпты

```python
# Модифицируйте system prompt
def _build_system_prompt(self, ...):
    prompt = f"""
    Ты - {self.custom_role}
    
    Специализация: {self.specialization}
    
    Твои уникальные правила:
    {self.custom_rules}
    """
    return prompt
```

### 3. Интеграция с внешними API

```python
# Пример: GitHub интеграция
class GitHubTool(BaseTool):
    def __init__(self, config):
        super().__init__(config)
        self.client = Github(config['token'])
    
    async def execute(self, input_data):
        if input_data['action'] == 'list_repos':
            repos = self.client.get_user().get_repos()
            return [repo.name for repo in repos]
```

### 4. Плагин система

```python
# plugins/my_plugin.py
class MyPlugin:
    def __init__(self, agent):
        self.agent = agent
    
    async def on_message(self, message):
        # Обработка каждого сообщения
        pass
    
    async def on_response(self, response):
        # Обработка каждого ответа
        pass

# Регистрация
agent.register_plugin(MyPlugin(agent))
```

---

## 🎯 Реальные use-cases

### Use case 1: Daily Standup

```python
daily_prompt = """
Сгенерируй мой daily standup на основе:
1. Завершенных задач за вчера
2. Активных задач на сегодня
3. Блокеров (задачи с тегом 'blocked')

Формат:
Yesterday: ...
Today: ...
Blockers: ...
"""
```

### Use case 2: Code Review Assistant

```python
review_prompt = """
Проанализируй PR:
1. Прочитай diff файлы в ./pr_diff/
2. Проверь code style guidelines (в моей базе знаний)
3. Найди потенциальные баги
4. Предложи улучшения
5. Создай задачи для критичных находок
"""
```

### Use case 3: Learning Journal

```python
learning_prompt = """
Я только что изучил {topic}.

Сохрани это в базу знаний с тегами.
Создай задачи для практики.
Предложи связанные темы для изучения.
"""
```

### Use case 4: Sprint Planning

```python
sprint_prompt = """
Помоги спланировать спринт:
1. Покажи backlog задач
2. Оцени сложность каждой (из описания и моего прошлого опыта)
3. Предложи распределение на 2 недели
4. Создай задачи для каждого дня
"""
```

---

## 🔥 Pro Tips

### 1. Алиасы для частых команд

```bash
# ~/.bashrc или ~/.zshrc
alias ga='python /path/to/god_agent/cli.py'
alias ga-start='ga start'
alias ga-tasks='ga task-list'
alias ga-stats='ga stats'
```

### 2. Утренний ритуал

```bash
#!/bin/bash
# morning_routine.sh

echo "☀️ Доброе утро! Загружаю God Agent..."

# Статистика
python cli.py stats

# Задачи на сегодня
echo "\n📋 Задачи на сегодня:"
python cli.py task-list --status todo

# Просроченные
echo "\n⚠️ Просроченные задачи:"
python cli.py task-list --overdue
```

### 3. Pre-commit hook

```bash
# .git/hooks/pre-commit

#!/bin/bash
python /path/to/god_agent/cli.py start << EOF
Проанализируй изменения в git diff и:
1. Найди TODO без issue номеров
2. Проверь code style
3. Предупреди о потенциальных проблемах
EOF
```

### 4. Еженедельный отчет

```python
# weekly_report.py
weekly_prompt = """
Сгенерируй еженедельный отчет:

1. Завершенные задачи (по приоритетам)
2. Время на категориях (по тегам)
3. Топ достижений
4. Что можно улучшить на следующей неделе
5. План на следующую неделю
"""
```

---

## 🐛 Troubleshooting

### Проблема: Slow RAG search

**Решение:**
```yaml
rag:
  chunk_size: 500  # Уменьшите
  top_k: 3         # Уменьшите
```

### Проблема: Out of memory

**Решение:**
```yaml
memory:
  short_term:
    max_messages: 10  # Уменьшите
```

### Проблема: Voice не работает

**Решение:**
```bash
# Проверьте микрофон
python -c "import pyaudio; print('OK')"

# Переустановите зависимости
pip install --force-reinstall pyaudio
```

### Проблема: API rate limits

**Решение:**
```python
# Добавьте задержки
import asyncio
await asyncio.sleep(1)  # Между запросами
```

---

## 📖 Дополнительные ресурсы

- [Claude API Docs](https://docs.anthropic.com/)
- [Whisper API](https://platform.openai.com/docs/guides/speech-to-text)
- [FAISS](https://github.com/facebookresearch/faiss)
- [MCP Protocol](https://modelcontextprotocol.io/)

---

**Успехов с God Agent! 🚀**
